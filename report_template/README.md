# Report Template

Use `report.tex` as a starting point for the project report. The final report should be 4--6 pages excluding references.

Compile with:

```bash
pdflatex report.tex
bibtex report
pdflatex report.tex
pdflatex report.tex
```
